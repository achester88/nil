Defines a square function, call it and outputs the results

/*
)
  ;count + 1 = count
    )
      ;{output} count
    ) else nif count % 5 == 0 (
      ;{output} 0
    nif count % 5 != 0 (

;noop count > 100 (

;1 = count
;count num
*/
